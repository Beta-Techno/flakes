# SOPS configuration for secrets management
creation_rules:
  - path_regex: ^secrets/prod\.ya?ml$
    age:
      - age1ce09dcypnk45npaskswc0x3sngddz0cm34ln009jy54nwpa6ffwsjm5zn9  # netbox-01 (real key)
    # keep everything encrypted; no plaintext fields
